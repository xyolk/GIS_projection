/** pages/custom/diner_social/index.wxss */
page {
  height: 100vh;
  background-color: #f5f5f5;
  font-family: "Songti SC", "SimSun", "STSong", "serif";
  overflow: hidden;
}

.page-container {
  height: 100%;
  display: flex;
  flex-direction: column;
}

/* 顶部导航栏 */
.custom-nav-bar {
  background-color: white;
  height: 80rpx;

  box-sizing: content-box; 
  
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding-left: 30rpx;
  padding-right: 30rpx;
  box-shadow: 0 4rpx 10rpx rgba(0,0,0,0.05);
  z-index: 10; /* 保证层级最高 */

  /* flex-shrink: 0 防止被挤压 */
  flex-shrink: 0; 
}

.nav-title {
  font-size: 36rpx;
  font-weight: bold;
}

/* 滚动区域 */
.scroll-content {
  flex: 1;
  width: 100%;
  /* 垂直滚动 */
  overflow-y: auto; 
  -webkit-overflow-scrolling: touch; 
  
  /* 防止最底部的内容（如菜单列表）被 fixed 定位的 TabBar 遮挡 */
  padding-bottom: calc(130rpx + env(safe-area-inset-bottom));
}

/* 隐藏滚动条 (可选优化，让界面更像原生 App) */
.scroll-content::-webkit-scrollbar {
  width: 0;
  height: 0;
  color: transparent;
  display: none;
}

/* 用户卡片 */
.profile-card {
  /* 1. 布局与间距：恢复正常边距，添加内边距 */
  margin: 30rpx; /* 上下左右都有边距 */
  padding: 40rpx 30rpx; /* 卡片内部留白，让内容不顶边 */
  
  /* 2. 背景装饰：原 header-bg 的效果移到这里 */
  background: linear-gradient(135deg, #ff9800 0%, #f57c00 100%); /* 稍微提亮一点橙色 */
  border-radius: 32rpx; /* 完整的圆角矩形 */
  
  /* 3. 视觉优化 */
  box-shadow: 0 8rpx 20rpx rgba(245, 124, 0, 0.25); 
  color: white; 
}

.profile-layout {
  display: flex;
  align-items: center;
}

.avatar-wrapper {
  width: 140rpx;
  height: 140rpx;
  background-color: white;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;

 /* 边框与阴影 */
  border: 4rpx solid #ffffff; 
  box-shadow: 0 2rpx 8rpx rgba(0,0,0,0.05);

  /* 新增：清除 button 默认样式的代码 */
  padding: 0 !important;
  margin: 0 !important;
  line-height: normal !important;
  box-sizing: content-box !important;
}

/* 去除 button 自带的边框伪类 */
.avatar-wrapper::after {
  border: none;
}

/* 新增：头像图片的样式 */
.avatar-img {
  width: 100%;
  height: 100%;
  border-radius: 50%;
  display: block; /* 防止图片底部留白 */
}

.profile-info {
  margin-left: 30rpx;
}

.name-row {
  display: flex;
  align-items: center;
  gap: 16rpx;
  margin-bottom: 10rpx;
}

.user-name {
  font-size: 36rpx;
  font-weight: bold;
}

.level-tag {
  background-color: #fdd835;
  color: #333;
  font-size: 20rpx;
  padding: 2rpx 12rpx;
  border-radius: 20rpx;
  font-weight: bold;
}

.location-row {
  display: flex;
  align-items: center;
  font-size: 24rpx;
  opacity: 0.9;
  gap: 8rpx;
}

/* 功能网格 */
.function-grid {
  background-color: white;
  margin: 0 30rpx 20rpx 30rpx;
  border-radius: 24rpx;
  padding: 30rpx;
  box-shadow: 0 2rpx 10rpx rgba(0,0,0,0.05);
}

.grid-row {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 20rpx;
  margin-bottom: 30rpx;
}

.grid-item {
  display: flex;
  flex-direction: column;
  align-items: center;
}

.icon-circle {
  width: 100rpx;
  height: 100rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 16rpx;
  transition: all 0.3s;
}
.icon-circle:active { transform: scale(0.95); }

/* 图标背景色 */
.bg-orange { background-color: #fff3e0; }
.bg-blue { background-color: #e3f2fd; }
.bg-green { background-color: #e8f5e9; }
.bg-purple { background-color: #f3e5f5; }

.grid-text {
  font-size: 24rpx;
  color: #666;
}

.edit-btn {
  width: 100% !important;
  background-color: white !important;
  color: #ff9800 !important;
  border: 1px solid #ffcc80 !important;
  border-radius: 40rpx !important;
  font-size: 28rpx;
  padding: 10rpx 0;
}

/* 通用卡片样式 */
.section-card {
  background-color: white;
  margin: 0 30rpx 20rpx 30rpx;
  border-radius: 24rpx;
  padding: 30rpx;
  box-shadow: 0 2rpx 10rpx rgba(0,0,0,0.05);
}

.no-padding { padding: 0; overflow: hidden; }

.section-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 20rpx;
}

.section-title {
  font-size: 32rpx;
  font-weight: bold;
  color: #333;
}

.section-more {
  display: flex;
  align-items: center;
  font-size: 24rpx;
  color: #ff9800;
}

/* 最近浏览 */
.recent-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 20rpx;
}

.recent-item {
  background-color: #f9fafb;
  padding: 20rpx;
  border-radius: 16rpx;
}

.recent-img {
  width: 100%;
  height: 160rpx;
  border-radius: 12rpx;
  margin-bottom: 16rpx;
}
.bg-orange-light { background-color: #ffe0b2; }
.bg-teal-light { background-color: #b2dfdb; }
.bg-red-light { background-color: #ffcdd2; }
.bg-yellow-light { background-color: #fff9c4; }

.recent-name {
  font-size: 26rpx;
  font-weight: bold;
  color: #333;
  display: block;
  margin-bottom: 6rpx;
}

.recent-rating {
  display: flex;
  align-items: center;
  font-size: 22rpx;
  color: #666;
}

/* 口味分析进度条 */
.progress-list {
  display: flex;
  flex-direction: column;
  gap: 24rpx;
}

.progress-label {
  display: flex;
  justify-content: space-between;
  font-size: 26rpx;
  color: #666;
  margin-bottom: 8rpx;
}
.progress-val { color: #333; font-weight: bold; }

.progress-track {
  width: 100%;
  height: 16rpx;
  background-color: #f0f0f0;
  border-radius: 8rpx;
  overflow: hidden;
}

.progress-bar {
  height: 100%;
  background-color: #ff9800;
  border-radius: 8rpx;
}

/* 我的动态 */
.card-header-border {
  padding: 30rpx;
  font-size: 32rpx;
  font-weight: bold;
  color: #333;
  border-bottom: 1px solid #f0f0f0;
}

.feed-content { padding: 30rpx; }

.feed-row { display: flex; gap: 20rpx; }

.feed-avatar {
  width: 80rpx;
  height: 80rpx;
  background-color: #fff3e0;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.feed-main { flex: 1; }

.feed-user { font-size: 28rpx; font-weight: bold; color: #333; }

.feed-text { font-size: 26rpx; color: #666; margin: 8rpx 0; line-height: 1.4; }

.feed-images {
  display: flex;
  gap: 10rpx;
  margin-top: 10rpx;
}

.feed-img {
  width: 140rpx;
  height: 140rpx;
  border-radius: 12rpx;
}

.feed-footer {
  display: flex;
  justify-content: space-between;
  margin-top: 16rpx;
  font-size: 22rpx;
  color: #999;
}
.ml-2 { margin-left: 20rpx; }

/* 菜单列表 */
.menu-list {
  background-color: white;
  margin: 10rpx 30rpx;
  border-radius: 24rpx;
  padding: 10rpx 30rpx;
}

.menu-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 30rpx 0;
  border-bottom: 1px solid #f5f5f5;
}
.menu-item:last-child { border-bottom: none; }

.menu-left { display: flex; align-items: center; gap: 20rpx; }
.menu-text { font-size: 28rpx; font-weight: 500; color: #333; }

/* 底部 TabBar */
.tab-bar {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  height: 110rpx;
  background-color: white;
  border-top: 1rpx solid #e5e7eb;
  display: flex;
  padding-bottom: env(safe-area-inset-bottom);
  z-index: 100;
}

.tab-item {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  color: #9ca3af;
}

.tab-item.active {
  color: #ff9800;
}

.tab-text {
  font-size: 20rpx;
  margin-top: 6rpx;
}


/* =========================== */
/* 编辑资料弹窗样式       */
/* =========================== */

/* 1. 黑色半透明蒙层 */
.modal-mask {
  position: fixed;
  top: 0; left: 0; right: 0; bottom: 0;
  background: rgba(0, 0, 0, 0.5);
  z-index: 900; /* 确保盖住页面内容 */
  opacity: 0;
  visibility: hidden;
  transition: all 0.3s;
}
.modal-mask.show {
  opacity: 1;
  visibility: visible;
}

/* 2. 底部白色面板 */
.modal-panel {
  position: fixed;
  bottom: 0; left: 0; right: 0;
  background: white;
  border-radius: 32rpx 32rpx 0 0; /* 上方圆角 */
  padding: 40rpx 40rpx calc(40rpx + env(safe-area-inset-bottom)); /* 适配底部安全区 */
  z-index: 999; /* 最高层级 */
  
  /* 动画效果 */
  transform: translateY(100%); /* 默认藏在屏幕下面 */
  transition: transform 0.3s cubic-bezier(0.25, 1, 0.5, 1);
}
.modal-panel.show {
  transform: translateY(0); /* 滑上来 */
}

/* 3. 弹窗内部布局 */
.modal-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 40rpx;
}

.modal-title {
  font-size: 34rpx;
  font-weight: bold;
  color: #333;
}

/* 表单项 */
.form-item {
  margin-bottom: 30rpx;
}

.form-label {
  font-size: 26rpx;
  color: #666;
  margin-bottom: 12rpx;
  display: block;
}

/* 输入框通用样式 */
.form-input, .form-picker {
  height: 88rpx;
  background: #f5f5f5; /* 浅灰底色 */
  border-radius: 16rpx;
  padding: 0 24rpx;
  font-size: 30rpx;
  color: #333;
  display: flex;
  align-items: center; /* 让文字垂直居中 */
}

/* Picker 特有样式 */
.form-picker {
  justify-content: space-between;
}
.placeholder-text {
  color: #999;
}

/* 保存按钮 */
.save-btn {
  background: linear-gradient(90deg, #ff9800, #f57c00);
  color: white;
  border-radius: 44rpx;
  font-size: 32rpx;
  font-weight: bold;
  margin-top: 40rpx;
  border: none;
}
/* 去除按钮默认边框 */
.save-btn::after { border: none; }

.feed-hover {
  background-color: #f9f9f9; 
  transition: background-color 0.2s;
}